syntax = "proto3";

package proto;

import "move_classic.proto";
import "move_kaboom.proto";

// Move represents a chess move, which can be either a classic chess move or a kaboom chess move.
message Move {
  oneof move {
    ClassicChessMove classic_move = 1;
    KaboomChessMove kaboom_move = 2;
  }

}
