"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v3.19.6
// source: game.proto
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Turn = exports.PlayerColor = exports.Board = exports.Player = exports.Game = exports.protobufPackage = void 0;
/* eslint-disable */
const minimal_1 = __importDefault(require("protobufjs/minimal"));
const color_1 = require("./color");
const effect_1 = require("./effect");
const intent_1 = require("./intent");
const piece_1 = require("./piece");
exports.protobufPackage = "kaboomproto";
function createBaseGame() {
    return { uuid: "", rulesVariant: "", boards: [], players: [], pieces: [], turns: [] };
}
exports.Game = {
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.uuid !== "") {
            writer.uint32(10).string(message.uuid);
        }
        if (message.rulesVariant !== "") {
            writer.uint32(18).string(message.rulesVariant);
        }
        for (const v of message.boards) {
            exports.Board.encode(v, writer.uint32(162).fork()).ldelim();
        }
        for (const v of message.players) {
            exports.Player.encode(v, writer.uint32(242).fork()).ldelim();
        }
        for (const v of message.pieces) {
            piece_1.ChessPiece.encode(v, writer.uint32(322).fork()).ldelim();
        }
        for (const v of message.turns) {
            exports.Turn.encode(v, writer.uint32(402).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGame();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.uuid = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.rulesVariant = reader.string();
                    continue;
                case 20:
                    if (tag !== 162) {
                        break;
                    }
                    message.boards.push(exports.Board.decode(reader, reader.uint32()));
                    continue;
                case 30:
                    if (tag !== 242) {
                        break;
                    }
                    message.players.push(exports.Player.decode(reader, reader.uint32()));
                    continue;
                case 40:
                    if (tag !== 322) {
                        break;
                    }
                    message.pieces.push(piece_1.ChessPiece.decode(reader, reader.uint32()));
                    continue;
                case 50:
                    if (tag !== 402) {
                        break;
                    }
                    message.turns.push(exports.Turn.decode(reader, reader.uint32()));
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            uuid: isSet(object.uuid) ? globalThis.String(object.uuid) : "",
            rulesVariant: isSet(object.rulesVariant) ? globalThis.String(object.rulesVariant) : "",
            boards: globalThis.Array.isArray(object === null || object === void 0 ? void 0 : object.boards) ? object.boards.map((e) => exports.Board.fromJSON(e)) : [],
            players: globalThis.Array.isArray(object === null || object === void 0 ? void 0 : object.players) ? object.players.map((e) => exports.Player.fromJSON(e)) : [],
            pieces: globalThis.Array.isArray(object === null || object === void 0 ? void 0 : object.pieces) ? object.pieces.map((e) => piece_1.ChessPiece.fromJSON(e)) : [],
            turns: globalThis.Array.isArray(object === null || object === void 0 ? void 0 : object.turns) ? object.turns.map((e) => exports.Turn.fromJSON(e)) : [],
        };
    },
    toJSON(message) {
        var _a, _b, _c, _d;
        const obj = {};
        if (message.uuid !== "") {
            obj.uuid = message.uuid;
        }
        if (message.rulesVariant !== "") {
            obj.rulesVariant = message.rulesVariant;
        }
        if ((_a = message.boards) === null || _a === void 0 ? void 0 : _a.length) {
            obj.boards = message.boards.map((e) => exports.Board.toJSON(e));
        }
        if ((_b = message.players) === null || _b === void 0 ? void 0 : _b.length) {
            obj.players = message.players.map((e) => exports.Player.toJSON(e));
        }
        if ((_c = message.pieces) === null || _c === void 0 ? void 0 : _c.length) {
            obj.pieces = message.pieces.map((e) => piece_1.ChessPiece.toJSON(e));
        }
        if ((_d = message.turns) === null || _d === void 0 ? void 0 : _d.length) {
            obj.turns = message.turns.map((e) => exports.Turn.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return exports.Game.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c, _d, _e, _f;
        const message = createBaseGame();
        message.uuid = (_a = object.uuid) !== null && _a !== void 0 ? _a : "";
        message.rulesVariant = (_b = object.rulesVariant) !== null && _b !== void 0 ? _b : "";
        message.boards = ((_c = object.boards) === null || _c === void 0 ? void 0 : _c.map((e) => exports.Board.fromPartial(e))) || [];
        message.players = ((_d = object.players) === null || _d === void 0 ? void 0 : _d.map((e) => exports.Player.fromPartial(e))) || [];
        message.pieces = ((_e = object.pieces) === null || _e === void 0 ? void 0 : _e.map((e) => piece_1.ChessPiece.fromPartial(e))) || [];
        message.turns = ((_f = object.turns) === null || _f === void 0 ? void 0 : _f.map((e) => exports.Turn.fromPartial(e))) || [];
        return message;
    },
};
function createBasePlayer() {
    return { uuid: "", name: "" };
}
exports.Player = {
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.uuid !== "") {
            writer.uint32(10).string(message.uuid);
        }
        if (message.name !== "") {
            writer.uint32(18).string(message.name);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePlayer();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.uuid = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.name = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            uuid: isSet(object.uuid) ? globalThis.String(object.uuid) : "",
            name: isSet(object.name) ? globalThis.String(object.name) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.uuid !== "") {
            obj.uuid = message.uuid;
        }
        if (message.name !== "") {
            obj.name = message.name;
        }
        return obj;
    },
    create(base) {
        return exports.Player.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b;
        const message = createBasePlayer();
        message.uuid = (_a = object.uuid) !== null && _a !== void 0 ? _a : "";
        message.name = (_b = object.name) !== null && _b !== void 0 ? _b : "";
        return message;
    },
};
function createBaseBoard() {
    return { uuid: "", winningPlayerUuid: "", playerColors: [] };
}
exports.Board = {
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.uuid !== "") {
            writer.uint32(10).string(message.uuid);
        }
        if (message.winningPlayerUuid !== "") {
            writer.uint32(42).string(message.winningPlayerUuid);
        }
        for (const v of message.playerColors) {
            exports.PlayerColor.encode(v, writer.uint32(82).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseBoard();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.uuid = reader.string();
                    continue;
                case 5:
                    if (tag !== 42) {
                        break;
                    }
                    message.winningPlayerUuid = reader.string();
                    continue;
                case 10:
                    if (tag !== 82) {
                        break;
                    }
                    message.playerColors.push(exports.PlayerColor.decode(reader, reader.uint32()));
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            uuid: isSet(object.uuid) ? globalThis.String(object.uuid) : "",
            winningPlayerUuid: isSet(object.winningPlayerUuid) ? globalThis.String(object.winningPlayerUuid) : "",
            playerColors: globalThis.Array.isArray(object === null || object === void 0 ? void 0 : object.playerColors)
                ? object.playerColors.map((e) => exports.PlayerColor.fromJSON(e))
                : [],
        };
    },
    toJSON(message) {
        var _a;
        const obj = {};
        if (message.uuid !== "") {
            obj.uuid = message.uuid;
        }
        if (message.winningPlayerUuid !== "") {
            obj.winningPlayerUuid = message.winningPlayerUuid;
        }
        if ((_a = message.playerColors) === null || _a === void 0 ? void 0 : _a.length) {
            obj.playerColors = message.playerColors.map((e) => exports.PlayerColor.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return exports.Board.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c;
        const message = createBaseBoard();
        message.uuid = (_a = object.uuid) !== null && _a !== void 0 ? _a : "";
        message.winningPlayerUuid = (_b = object.winningPlayerUuid) !== null && _b !== void 0 ? _b : "";
        message.playerColors = ((_c = object.playerColors) === null || _c === void 0 ? void 0 : _c.map((e) => exports.PlayerColor.fromPartial(e))) || [];
        return message;
    },
};
function createBasePlayerColor() {
    return { playerUuid: "", color: 0 };
}
exports.PlayerColor = {
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.playerUuid !== "") {
            writer.uint32(10).string(message.playerUuid);
        }
        if (message.color !== 0) {
            writer.uint32(16).int32(message.color);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePlayerColor();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.playerUuid = reader.string();
                    continue;
                case 2:
                    if (tag !== 16) {
                        break;
                    }
                    message.color = reader.int32();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            playerUuid: isSet(object.playerUuid) ? globalThis.String(object.playerUuid) : "",
            color: isSet(object.color) ? (0, color_1.colorFromJSON)(object.color) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.playerUuid !== "") {
            obj.playerUuid = message.playerUuid;
        }
        if (message.color !== 0) {
            obj.color = (0, color_1.colorToJSON)(message.color);
        }
        return obj;
    },
    create(base) {
        return exports.PlayerColor.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b;
        const message = createBasePlayerColor();
        message.playerUuid = (_a = object.playerUuid) !== null && _a !== void 0 ? _a : "";
        message.color = (_b = object.color) !== null && _b !== void 0 ? _b : 0;
        return message;
    },
};
function createBaseTurn() {
    return { uuid: "", playerUuid: "", intents: [], effects: [] };
}
exports.Turn = {
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.uuid !== "") {
            writer.uint32(10).string(message.uuid);
        }
        if (message.playerUuid !== "") {
            writer.uint32(18).string(message.playerUuid);
        }
        for (const v of message.intents) {
            intent_1.Intent.encode(v, writer.uint32(82).fork()).ldelim();
        }
        for (const v of message.effects) {
            effect_1.Effect.encode(v, writer.uint32(90).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseTurn();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.uuid = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.playerUuid = reader.string();
                    continue;
                case 10:
                    if (tag !== 82) {
                        break;
                    }
                    message.intents.push(intent_1.Intent.decode(reader, reader.uint32()));
                    continue;
                case 11:
                    if (tag !== 90) {
                        break;
                    }
                    message.effects.push(effect_1.Effect.decode(reader, reader.uint32()));
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            uuid: isSet(object.uuid) ? globalThis.String(object.uuid) : "",
            playerUuid: isSet(object.playerUuid) ? globalThis.String(object.playerUuid) : "",
            intents: globalThis.Array.isArray(object === null || object === void 0 ? void 0 : object.intents) ? object.intents.map((e) => intent_1.Intent.fromJSON(e)) : [],
            effects: globalThis.Array.isArray(object === null || object === void 0 ? void 0 : object.effects) ? object.effects.map((e) => effect_1.Effect.fromJSON(e)) : [],
        };
    },
    toJSON(message) {
        var _a, _b;
        const obj = {};
        if (message.uuid !== "") {
            obj.uuid = message.uuid;
        }
        if (message.playerUuid !== "") {
            obj.playerUuid = message.playerUuid;
        }
        if ((_a = message.intents) === null || _a === void 0 ? void 0 : _a.length) {
            obj.intents = message.intents.map((e) => intent_1.Intent.toJSON(e));
        }
        if ((_b = message.effects) === null || _b === void 0 ? void 0 : _b.length) {
            obj.effects = message.effects.map((e) => effect_1.Effect.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return exports.Turn.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c, _d;
        const message = createBaseTurn();
        message.uuid = (_a = object.uuid) !== null && _a !== void 0 ? _a : "";
        message.playerUuid = (_b = object.playerUuid) !== null && _b !== void 0 ? _b : "";
        message.intents = ((_c = object.intents) === null || _c === void 0 ? void 0 : _c.map((e) => intent_1.Intent.fromPartial(e))) || [];
        message.effects = ((_d = object.effects) === null || _d === void 0 ? void 0 : _d.map((e) => effect_1.Effect.fromPartial(e))) || [];
        return message;
    },
};
function isSet(value) {
    return value !== null && value !== undefined;
}
//# sourceMappingURL=game.js.map