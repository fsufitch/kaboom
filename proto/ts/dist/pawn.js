"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v3.19.6
// source: pawn.proto
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.PawnKaboom = exports.PawnBump = exports.PawnMove = exports.protobufPackage = void 0;
/* eslint-disable */
const minimal_1 = __importDefault(require("protobufjs/minimal"));
const piece_1 = require("./piece");
exports.protobufPackage = "kaboomproto";
function createBasePawnMove() {
    return { fromRow: 0, fromCol: 0, toRow: 0, toCol: 0, promotion: 0 };
}
exports.PawnMove = {
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.fromRow !== 0) {
            writer.uint32(8).int32(message.fromRow);
        }
        if (message.fromCol !== 0) {
            writer.uint32(16).int32(message.fromCol);
        }
        if (message.toRow !== 0) {
            writer.uint32(24).int32(message.toRow);
        }
        if (message.toCol !== 0) {
            writer.uint32(32).int32(message.toCol);
        }
        if (message.promotion !== 0) {
            writer.uint32(40).int32(message.promotion);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePawnMove();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 8) {
                        break;
                    }
                    message.fromRow = reader.int32();
                    continue;
                case 2:
                    if (tag !== 16) {
                        break;
                    }
                    message.fromCol = reader.int32();
                    continue;
                case 3:
                    if (tag !== 24) {
                        break;
                    }
                    message.toRow = reader.int32();
                    continue;
                case 4:
                    if (tag !== 32) {
                        break;
                    }
                    message.toCol = reader.int32();
                    continue;
                case 5:
                    if (tag !== 40) {
                        break;
                    }
                    message.promotion = reader.int32();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            fromRow: isSet(object.fromRow) ? globalThis.Number(object.fromRow) : 0,
            fromCol: isSet(object.fromCol) ? globalThis.Number(object.fromCol) : 0,
            toRow: isSet(object.toRow) ? globalThis.Number(object.toRow) : 0,
            toCol: isSet(object.toCol) ? globalThis.Number(object.toCol) : 0,
            promotion: isSet(object.promotion) ? (0, piece_1.pieceTypeFromJSON)(object.promotion) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.fromRow !== 0) {
            obj.fromRow = Math.round(message.fromRow);
        }
        if (message.fromCol !== 0) {
            obj.fromCol = Math.round(message.fromCol);
        }
        if (message.toRow !== 0) {
            obj.toRow = Math.round(message.toRow);
        }
        if (message.toCol !== 0) {
            obj.toCol = Math.round(message.toCol);
        }
        if (message.promotion !== 0) {
            obj.promotion = (0, piece_1.pieceTypeToJSON)(message.promotion);
        }
        return obj;
    },
    create(base) {
        return exports.PawnMove.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c, _d, _e;
        const message = createBasePawnMove();
        message.fromRow = (_a = object.fromRow) !== null && _a !== void 0 ? _a : 0;
        message.fromCol = (_b = object.fromCol) !== null && _b !== void 0 ? _b : 0;
        message.toRow = (_c = object.toRow) !== null && _c !== void 0 ? _c : 0;
        message.toCol = (_d = object.toCol) !== null && _d !== void 0 ? _d : 0;
        message.promotion = (_e = object.promotion) !== null && _e !== void 0 ? _e : 0;
        return message;
    },
};
function createBasePawnBump() {
    return { fromRow: 0, fromCol: 0, toRow: 0, toCol: 0, promotion: 0 };
}
exports.PawnBump = {
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.fromRow !== 0) {
            writer.uint32(8).int32(message.fromRow);
        }
        if (message.fromCol !== 0) {
            writer.uint32(16).int32(message.fromCol);
        }
        if (message.toRow !== 0) {
            writer.uint32(24).int32(message.toRow);
        }
        if (message.toCol !== 0) {
            writer.uint32(32).int32(message.toCol);
        }
        if (message.promotion !== 0) {
            writer.uint32(40).int32(message.promotion);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePawnBump();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 8) {
                        break;
                    }
                    message.fromRow = reader.int32();
                    continue;
                case 2:
                    if (tag !== 16) {
                        break;
                    }
                    message.fromCol = reader.int32();
                    continue;
                case 3:
                    if (tag !== 24) {
                        break;
                    }
                    message.toRow = reader.int32();
                    continue;
                case 4:
                    if (tag !== 32) {
                        break;
                    }
                    message.toCol = reader.int32();
                    continue;
                case 5:
                    if (tag !== 40) {
                        break;
                    }
                    message.promotion = reader.int32();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            fromRow: isSet(object.fromRow) ? globalThis.Number(object.fromRow) : 0,
            fromCol: isSet(object.fromCol) ? globalThis.Number(object.fromCol) : 0,
            toRow: isSet(object.toRow) ? globalThis.Number(object.toRow) : 0,
            toCol: isSet(object.toCol) ? globalThis.Number(object.toCol) : 0,
            promotion: isSet(object.promotion) ? (0, piece_1.pieceTypeFromJSON)(object.promotion) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.fromRow !== 0) {
            obj.fromRow = Math.round(message.fromRow);
        }
        if (message.fromCol !== 0) {
            obj.fromCol = Math.round(message.fromCol);
        }
        if (message.toRow !== 0) {
            obj.toRow = Math.round(message.toRow);
        }
        if (message.toCol !== 0) {
            obj.toCol = Math.round(message.toCol);
        }
        if (message.promotion !== 0) {
            obj.promotion = (0, piece_1.pieceTypeToJSON)(message.promotion);
        }
        return obj;
    },
    create(base) {
        return exports.PawnBump.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c, _d, _e;
        const message = createBasePawnBump();
        message.fromRow = (_a = object.fromRow) !== null && _a !== void 0 ? _a : 0;
        message.fromCol = (_b = object.fromCol) !== null && _b !== void 0 ? _b : 0;
        message.toRow = (_c = object.toRow) !== null && _c !== void 0 ? _c : 0;
        message.toCol = (_d = object.toCol) !== null && _d !== void 0 ? _d : 0;
        message.promotion = (_e = object.promotion) !== null && _e !== void 0 ? _e : 0;
        return message;
    },
};
function createBasePawnKaboom() {
    return { row: 0, col: 0 };
}
exports.PawnKaboom = {
    encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.row !== 0) {
            writer.uint32(8).int32(message.row);
        }
        if (message.col !== 0) {
            writer.uint32(16).int32(message.col);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePawnKaboom();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 8) {
                        break;
                    }
                    message.row = reader.int32();
                    continue;
                case 2:
                    if (tag !== 16) {
                        break;
                    }
                    message.col = reader.int32();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            row: isSet(object.row) ? globalThis.Number(object.row) : 0,
            col: isSet(object.col) ? globalThis.Number(object.col) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.row !== 0) {
            obj.row = Math.round(message.row);
        }
        if (message.col !== 0) {
            obj.col = Math.round(message.col);
        }
        return obj;
    },
    create(base) {
        return exports.PawnKaboom.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b;
        const message = createBasePawnKaboom();
        message.row = (_a = object.row) !== null && _a !== void 0 ? _a : 0;
        message.col = (_b = object.col) !== null && _b !== void 0 ? _b : 0;
        return message;
    },
};
function isSet(value) {
    return value !== null && value !== undefined;
}
//# sourceMappingURL=pawn.js.map