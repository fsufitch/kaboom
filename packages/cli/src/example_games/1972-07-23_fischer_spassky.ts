import { GameSnapshot } from '@kaboom/proto';

export default GameSnapshot.create({
  // ... populate the snapshot with the game state from the Fischer vs. Spassky match on 1972-07-23
  // use the sibling text file as a reference for the full content
});
