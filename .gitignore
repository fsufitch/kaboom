# --- Bazel output / workspace state ---
bazel-*
**/bazel-*
.bazelrc.user
.bazelrc.local

# If you use Bazelisk but don't want to commit version pins in some contexts:
# (You probably *do* want to commit .bazelversion for reproducibility.)
# .bazelversion

# --- Compilation database / clang tooling ---
compile_commands.json
.compile_commands.json
.cache/
.clangd/
.clang-tidy-cache/
*.clang-tidy

# --- Build / coverage artifacts ---
*.gcda
*.gcno
*.profraw
*.profdata
coverage/
coverage.dat
lcov.info
*.lcov

# --- Logs / temp files ---
*.log
*.tmp
*.swp
*.swo
*~

# --- VS Code ---
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json

# --- JetBrains / CLion ---
.idea/
cmake-build-*/

# --- macOS / Windows junk ---
.DS_Store
Thumbs.db
desktop.ini

# --- Python (if you later add bindings) ---
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/

# --- Node (if you later add JS tooling) ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
